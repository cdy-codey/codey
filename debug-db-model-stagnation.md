[OPEN] db-model-stagnation

# 问题摘要

- 现象：启用数据库模型配置后，只要输入聊天信息，就返回 `Loop halted: detected stagnation`。
- 期望：聊天请求正常产生模型响应，不应在少量循环后被 `LoopGuard` 判定为停滞。

# 当前假设

- 假设 1：数据库配置未完整注入到会话 `modelConfig`，导致模型请求实际失败或回退。
- 假设 2：`ChatController.normalize()` 在消息阶段重复覆盖会话模型配置，造成会话状态漂移。
- 假设 3：模型返回了重复的工具调用指令，`LoopGuard` 因重复工具请求而拦截。
- 假设 4：运行时实际使用的模型配置与预期数据库配置不一致，命中了错误网关参数。

# 调试计划

- 先定位数据库配置注入点、会话模型配置传递链、`LoopGuard` 触发链路。
- 第一轮只加调试埋点，收集请求模型配置、会话模型配置、循环反馈信息。
- 复现问题后根据运行时证据确认根因，再做最小修复。

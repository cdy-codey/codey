package com.codey.tools;

import com.codey.tools.*;

/**
 * `edit_code` 支持的编辑模式。
 */
public enum EditMode {
    APPEND,
    INSERT_BEFORE,
    INSERT_AFTER,
    REPLACE_TEXT,
    REPLACE_BETWEEN_MARKERS,
    REPLACE_FILE
}

